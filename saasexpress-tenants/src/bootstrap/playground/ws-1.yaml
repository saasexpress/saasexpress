name: ws-1
nodes:
  - id: start
    action: HTTPIn
    config:
      ws: true
      method: ^(ANY)$
      routes:
        - /ws1

  - id: rp
    action: APICall
    config:
      ws: true
      path: /ws-dest
      method: GET
      url: http://localhost:2500

edges:
  - { from: start, to: rp }
