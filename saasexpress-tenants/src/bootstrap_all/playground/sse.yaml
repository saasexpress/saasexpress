name: sse
nodes:
  - id: start
    operator: HTTPIn
    config:
      sse: true
      method: ^(GET)$
      routes:
        - /sse

  - id: rp
    operator: APICall
    config:
      sse: true
      path: /sse-source
      url: http://localhost:2243

edges:
  - { from: start, to: rp }
