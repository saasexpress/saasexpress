name: sse
nodes:
  - id: start
    operator: HTTPIn
    sse: true
    method: ^(GET)$
    routes:
      - /sse

  - id: rp
    operator: APICall
    sse: true
    path: /sse-source
    url: http://localhost:2243

edges:
  - { from: start, to: rp }
