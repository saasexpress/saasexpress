name: sse
nodes:
  - id: start
    action: HTTPIn
    config:
      sse: true
      method: ^(GET)$
      routes:
        - /sse

  - id: rp
    action: APICall
    config:
      sse: true
      path: /sse-source
      url: http://localhost:2243

edges:
  - { from: start, to: rp }
