name: hello-world
nodes:
  - id: start
    operator: HTTPIn
    method: ^(GET)$
    routes:
      - /hello

  - id: translate
    operator: MessageTranslator
    mode: json
    template: |
      {
        "message": "Hello World!"
      }

edges:
  - { from: start, to: translate }
