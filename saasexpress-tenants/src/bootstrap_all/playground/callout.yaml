name: callout
nodes:
  - id: start
    operator: HTTPIn
    config:
      method: GET
      routes:
        - /callout

  - id: callout
    operator: Callout
    config:
      graph_name: worker
edges:
  - { from: start, to: callout }
