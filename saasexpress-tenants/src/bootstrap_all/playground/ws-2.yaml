name: ws-2
nodes:
  - id: start
    operator: HTTPIn
    ws: true
    method: ^(ANY)$
    routes:
      - /ws-dest

  - id: rp
    operator: APICall
    ws: true
    method: GET
    path: /ws-tweak
    url: http://localhost:2243

edges:
  - { from: start, to: rp }
