# DOES NOT WORK!

# curl -v http://localhost:2500/shell -d 'ls'
name: shell
nodes:
  - id: start
    action: HTTPIn
    config:
      method: POST
      routes:
        - /shell

  - id: shell_node
    action: Shell
    config:
      command: bash
      args:
        - "-i"

edges:
  - { from: start, to: shell_node }
