# DOES NOT WORK!

# curl -v http://localhost:2500/callout -d 'ls'
name: shell
nodes:
  - id: start
    action: HTTPIn
    config:
      method: GET
      routes:
        - /callout

  - id: callout
    action: Callout
    config:
      graph_name: vault

edges:
  - { from: start, to: callout }
