#
# - implement HTTPIn to match the route
#
name: tenant-ui
nodes:
  - id: start
    action: HTTPIn
    config:
      method: GET
      routes:
        - /ui/
        - /ui/{*rest}

  - id: rp
    action: APICall
    config:
      url: http://127.0.0.1:5173

edges:
  - { from: start, to: rp }
