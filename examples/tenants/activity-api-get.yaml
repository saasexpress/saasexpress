name: activity-api-get
nodes:
  - id: start
    action: HTTPIn
    config:
      method: ^(GET)$
      routes:
        - /api/activity

  - id: rp
    action: APICall
    config:
      url: http://localhost:8081

edges:
  - from: start
    to: rp
