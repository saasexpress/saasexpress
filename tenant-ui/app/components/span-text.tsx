import { TypographyProps } from "@mui/material";
import styled from "@emotion/styled";

const IkformSpanText = styled("span")(({}: TypographyProps) => ({
  fontSize: "1rem",
  color: "black",
  backgroundColor: "transparent",
  marginBottom: "0.5rem",
  // marginTop: 10,
  // marginBottom: 2,
}));

export default IkformSpanText;
